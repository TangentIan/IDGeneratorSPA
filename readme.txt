install node and npm globally to run this
in the command line:
cd into the folder that contains the grunt file
run "npm install" to resolve all the build dependencies
run "grunt build" 
run "grunt serve" to start up a server locally
